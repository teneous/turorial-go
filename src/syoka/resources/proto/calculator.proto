syntax = "proto3";

//缺省补偿
package calculator;

option go_package = "./grpc";

service Calculator {
  rpc Add (Element) returns (Result) {}
}

message Element {
  int32 a = 1;
  int32 b = 2;
}

message Result {
  int64 result = 1;
}